import 'package:flutter/material.dart';

class UrgentCampaignsList extends StatelessWidget {
  const UrgentCampaignsList({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
